services:
  api-test:
    build:
      context: ./api
      dockerfile: Dockerfile
      target: development
    container_name: futebol-api-test
    volumes:
      - ./api:/app
      # Set DATA_VOLUME in .env to point to your CSV directory
      - ${DATA_VOLUME}:/data:ro
    environment:
      - DATA_DIR=/data
      - FORCE_COLOR=1
    command:
      - sh
      - -c
      - "uv sync && PYTHONPATH=football_stats uv run pytest tests/ -v --color=yes"

  web-test:
    build:
      context: ./web
      dockerfile: Dockerfile
      target: development
    container_name: futebol-web-test
    volumes:
      - ./web:/app
    command:
      - sh
      - -c
      - "pnpm lint"
