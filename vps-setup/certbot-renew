# Let's Encrypt certificate auto-renewal
# Runs daily at 3 AM UTC
0 3 * * * root cd /opt/futebol && docker compose run --rm certbot renew --webroot -w /var/www/certbot && docker compose exec nginx nginx -s reload >> /var/log/certbot-renew.log 2>&1
